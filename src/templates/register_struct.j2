struct {{NAME}}register {

{% for register in registers.register %}
    Register<{{register.addressOffset}}, {{register.name}}register, {{register.resetValue}}> {{register.name}}_;
{% endfor %}

    {{NAME}}register(REG_ADDR base) : 
{% for register in registers.register %}
        {{register.name}}_{base}
{%- if not loop.last %}
,
{% else %}
 {};
{% endif %}
{% endfor %}
};
