
struct {{NAME}}_s {


{% for register in registers.register %}
    Register<{{register.addressOffset}}, {{register.name}}_s, {{register.resetValue}}> {{register.name}}_r;
{% endfor %}

    {{NAME}}_s(REG_ADDR base) : 
{% for register in registers.register %}
        {{register.name}}_r{base}
{%- if not loop.last %}
,
{% else %}
 {};
{% endif %}
{% endfor %}
};
